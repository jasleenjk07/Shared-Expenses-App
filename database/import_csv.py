"""
Legacy script — use the app's Import tab or scripts/run_import_test.py instead.

The full import engine with anomaly detection lives in:
  backend/services/import_service.py
"""

if __name__ == "__main__":
    print("Use: PYTHONPATH=. python scripts/run_import_test.py")
    print("Or upload Expenses_Export.csv via the web UI Import tab.")
